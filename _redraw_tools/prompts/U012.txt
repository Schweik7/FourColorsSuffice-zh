# 重绘任务 U012（章节：第3章 欧拉的著名公式 / 从多面体到地图）

项目目录（工作目录）：
D:\0-code\0-python\FourColorTheorem\proof\papers\《四色足矣：四色猜想是如何解决的》（Robin Wilson）

## 第一步
阅读 `_redraw_tools/GUIDE.md`（统一设计规范，技术栈、配色、输出要求都在里面，必须遵守）。

## 本单元要重绘的图（共 3 张）
- fig-062  原文件名: fig-062_立方体投影到平面示意图.jpg  描述: 立方体投影到平面示意图  初步分类: projection_composite
- fig-063  原文件名: fig-063_正十二面体立体与投影对照图.jpg  描述: 正十二面体立体与投影对照图  初步分类: polyhedron
- fig-064  原文件名: fig-064_多面体投影对应地图示意图.jpg  描述: 多面体投影对应地图示意图  初步分类: polyhedron

**注意：本单元包含多张图，已被判定为可能属于同一逻辑场景（有拓扑一致性风险）。动笔前请完整阅读下面给出的正文段落，判断这些图到底是「同一对象的连续演化」（必须共享同一套底图/坐标/邻接关系，只改变着色或高亮状态）还是「同类但相互独立的个体」（可以分别自由设计）。**

## 这些图所在的正文原文（用于理解图的具体内容、判断逻辑链条、确定标签/颜色/邻接关系）
```
## 从多面体到地图

所有这些多面体公式与地图有什么关系呢？我们可以通过将多面体从单个点投影到一个平面（如桌面）上，来看出其中的联系。为此，我们首先将多面体“膨胀”成一个球面，再将球面投影到平面上，正如我们在第1章中解释的那样。下面的图示展示了立方体是如何以这种方式被投影的。

![](images/fig-062_立方体投影到平面示意图.jpg)

我们可以用同样的方式获得任何多面体的平面版本。下面的图示展示了正十二面体在其立体形式和投影形式下的样子。

![](images/fig-063_正十二面体立体与投影对照图.jpg)

1813年，奥古斯丁-路易·柯西（Augustin-Louis Cauchy）对多面体产生了兴趣。柯西是十九世纪上半叶法国首屈一指的数学家，他获得了微积分的第一个严格版本，并推动开创了代数中一个现今称为群论的重要领域。以他的名字命名的结果和概念可能比其他任何数学家都多。

柯西通过首先将多面体投影到一个平面上（如上所述）来证明欧拉公式。该公式对这些平面图仍然成立，前提是我们谨慎处理外部区域。柯西倾向于忽略这个外部区域（当我们将多面体压平时出现的那个区域），这样他就比以前少了一个区域。他陈述的欧拉公式因此变为 $F - E + V = 1$；例如，压扁的立方体有5个“内部”区域、12条棱和8个顶点，因此 $F - E + V = 5 - 12 + 8 = 1$。然而，如果我们将外部区域包括在内，正如我们在处理多面体投影时通常选择的那样，那么 $F = 6$，且 $F - E + V = 6 - 12 + 8 = 2$，和之前一样。


这就是联系所在。我们也可以将这些平面图视为地图——面对应于国家，棱对应于边界线，顶点对应于交汇点。

![](images/fig-064_多面体投影对应地图示意图.jpg)

欧拉公式于是采用以下形式：

```

## 你要做的事
1. 读 GUIDE.md 和上面的正文，必要时用 Read 工具打开 `images/` 目录下对应的原始 .jpg 看一眼原图构图（原图可能模糊但能看出大致布局）。
2. 用 Python（matplotlib，可复用 `_redraw_tools/polyhedra.py` 和 `_redraw_tools/solids.py`）为每一张图生成清晰的 SVG。
3. 把每张 SVG 保存到 `images/` 目录，文件名把对应原文件名的 `.jpg` 换成 `.svg`，其余不变（例如 `fig-062_立方体投影到平面示意图.jpg` → `fig-062_立方体投影到平面示意图.svg`）。
4. 完成后，用几句话总结：画了哪些图、做了什么内容判断、有没有拿不准的地方。不要修改 GUIDE.md、polyhedra.py、solids.py、work_units.json、或 FourColors_Suffice_zh.md。
